name: resume-parent
description: "Parent workflow for sub-workflow resume test"

steps:
  - id: run-sub
    workflow: e2e-sub-workflow-resume-child.yaml

  - id: parent-after
    mode: shell
    command: echo "parent-after-done"
